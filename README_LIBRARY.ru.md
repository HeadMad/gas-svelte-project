# 📚 [Название Проекта] - Google Apps Script Библиотека

> **[Название Проекта]** — это [Краткое описание: что это и для чего]. Библиотека решает проблему [описание проблемы] за счет [описание решения/преимуществ].

![License](https://img.shields.io/badge/license-MIT-blue.svg)![GAS](https://img.shields.io/badge/platform-Google%20Apps%20Script-green.svg)![Version](https://img.shields.io/badge/version-1.0.0-orange.svg)

<a name="toc"></a>
## 📖 Содержание

1. [🚀 Установка и настройка](#install)
2. [🚀 Быстрый старт](#quickstart)
3. [API: Основные методы](#core_methods)
4. [API: Вспомогательные классы](#sub_methods)
5. [Структура данных](#data_methods)
6. [Примеры использования](#examples)
7. [⚠️ Важные замечания](#notes)
8. [🎯 Советы по использованию](#tips)
9. [🐛 Известные ограничения](#limits)

---

<a name="install"></a>
## 🚀 Установка и настройка
[меню](#toc) | [далее](#quickstart) | [назад](#toc)

### Установите зависимости

```bash
npm install
```

### Настройте Clasp (для локальной разработки)

Установите Clasp глобально и авторизуйтесь:
```bash
npm install -g @google/clasp
clasp login
```

### Свяжите с проектом Google
Создайте файл `.clasp.json` в корне проекта:
```json
{
  "scriptId": "ВАШ_SCRIPT_ID",
  "rootDir": "./dist"
}
```

### Команды (Scripts)

| Команда | Описание |
| :--- | :--- |
| `npm run build` | Сборка проекта в папку `dist/`. |
| `npm run push` | Сборка + загрузка кода на Google Drive. |
| `npm run deploy` | Полный цикл релиза (Build -> Push -> Version). |

### Опции

Процесс сборки можно настроить через `build.config.json`.

| Опция | Тип | Описание | По умолчанию |
| :--- | :--- | :--- | :--- |
| `manifest` | `string` | Путь к файлу манифеста (`appsscript.json`). | `src/appsscript.json` |
| `package` | `string` | Путь к `package.json`. | `./package.json` |
| `outDir` | `string` | Выходной каталог для сборки. | `dist` |
| `frontend.build` | `boolean` | Если `true`, собирает фронтенд. | `true` |
| `frontend.src`| `string` | Исходный каталог для фронтенда. | `src/frontend` |
| `frontend.minify` | `boolean` | Если `true`, минифицирует код фронтенда. | `true` |
| `backend.build` | `boolean` | Если `true`, собирает бэкенд. | `true` |
| `backend.src` | `string` | Исходный каталог для бэкенда. | `src/backend` |
| `backend.concatenate`| `boolean`| Если `true`, объединяет файлы бэкенда. | `true` |
| `backend.outFile`| `string` | Имя объединенного выходного файла. | `Code.js` |
| `backend.minify` | `boolean` | Если `true`, минифицирует код бэкенда. | `true` |
| `backend.priorityOrder`| `string[]`| Массив файлов для первоочередного включения. | `[]` |
| `deployment.devDeploymentId` | `string` | ID развертывания для среды разработки (URL веб-приложения). | "" |
| `deployment.prodDeploymentId` | `string` | ID развертывания для производственной среды (URL веб-приложения). | "" |

---

### Подключение библиотеки в свой проект:

1. Откройте ваш проект Google Apps Script
2. Нажмите **Редактор** → **Библиотеки** (значок `+` слева)
3. Введите Script ID библиотеки: `[ВСТАВИТЬ_SCRIPT_ID_БИБЛИОТЕКИ]`
4. Нажмите **Найти**
5. Выберите последнюю версию
6. В поле "Идентификатор" укажите `[ShortName]` (например, `MyLib`)
7. Нажмите **Добавить**

---

<a name="quickstart"></a>
## 🚀 Быстрый старт
[меню](#toc) | [далее](#core_methods) | [назад](#install)

```javascript
function run() {
  // Инициализация
  const app = MyLib.init();
  
  // Вызов основного метода
  app.doSomething({
    option: 'value'
  });

  // Обработка результата
  const result = app.process();
  Logger.log(result);
}
```

---

<a name="core_methods"></a>
## API: Основные методы [Класс Main]
[меню](#toc) | [далее](#sub_methods) | [назад](#quickstart)

Описание статических методов или методов главного класса.

### `init(config)`
Основная точка входа.
**Параметры:**
- `config` _(object)_ - Объект конфигурации.

**Возвращает:** Экземпляр класса `Main`.

```javascript
const app = MyLib.init({ debug: true });
```

### `methodName(arg1, arg2)`
Краткое описание того, что делает метод.
- `arg1` _(type)_ - Описание аргумента.
- `arg2` _(type)_ - Описание аргумента.
- **Возвращает:** `Type`.

```javascript
app.methodName('value', 123);
```

---

<a name="sub_methods"></a>
## API: Вспомогательные классы [Класс Sub]
[меню](#toc) | [далее](#data_methods) | [назад](#core_methods)

Описание методов вторичных классов (если есть).

### `subMethod(handler)`
Описание метода.
**Параметры:**
- `handler` _(function)_: Функция обратного вызова.

**Возвращает:** `void`.

---

<a name="data_methods"></a>
## Структура данных
[меню](#toc) | [далее](#examples) | [назад](#sub_methods)

Описание объектов данных, которые возвращает библиотека или которые используются внутри.

### Объект `Config`
```javascript
{
  key: "value", // Описание поля
  isEnabled: true // Описание поля
}
```

---

<a name="examples"></a>
## 💡 Примеры использования
[меню](#toc) | [далее](#notes) | [назад](#data_methods)

### 1. Базовый сценарий
Описание самого простого сценария использования.

```javascript
function simpleUsage() {
  const lib = MyLib.init();
  lib.performAction();
}
```

### 2. Продвинутый сценарий (с опциями)
Описание более сложного кейса.

```javascript
function advancedUsage() {
  const lib = MyLib.init();
  
  try {
    lib.complexMethod((item) => {
      return item.value > 10;
    });
  } catch (e) {
    Logger.log('Error: ' + e.message);
  }
}
```

### 3. Интеграция с другими сервисами
Пример того, как библиотека работает в связке с Sheets, Gmail или Forms.

```javascript
// Ваш код здесь
```

---

<a name="notes"></a>
## ⚠️ Важные замечания
[меню](#toc) | [далее](#tips) | [назад](#examples)

1.  **Замечание 1**: Важный нюанс работы (например, необходимость включения API Google).
2.  **Замечание 2**: Поведение при ошибках.
3.  **Авторизация**: Какие скоупы (Scopes) требуются.

---

<a name="tips"></a>
## 🎯 Советы по использованию
[меню](#toc) | [далее](#limits) | [назад](#notes)

### Производительность
Советы по оптимизации кода при использовании библиотеки.

### Расширяемость
Как добавить свой функционал или плагины.

---

<a name="limits"></a>
## 🐛 Известные ограничения
[меню](#toc) | [назад](#tips)

1.  **Лимиты Google**: Библиотека подчиняется стандартным квотам GAS (время выполнения, URL Fetch и т.д.).
2.  **Специфическое ограничение**: Например, макс. размер файла или поддерживаемые форматы.